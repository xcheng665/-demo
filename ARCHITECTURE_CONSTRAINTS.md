# 技术架构约束与变更确认规则

## 1. 目的与适用范围

本文档定义“程志远建筑作品集”当前已确认的技术架构，以及后续变更时必须遵守的边界。它适用于本仓库内的网页、AI 分身、部署配置、数据资料和构建流程。

**首要规则：任何会改变底层架构、系统边界、数据流、部署方式或安全模型的工作，实施前必须先获得项目负责人确认。** 未获得确认时，只能进行只读检查、提出方案或完成不触及架构的内容/样式修改。

## 2. 当前基线架构

```text
访客浏览器
  └─ React 19 单页应用（Vite 构建）
       ├─ 页面与交互：src/App.tsx、src/components/
       ├─ 作品与展示数据：src/portfolioData.ts、public/assets/
       └─ AI 页面：AiChat → POST /api/chat
                               │
Cloudflare Pages ──────────────┘
  ├─ 静态站点：dist/
  └─ Pages Function：functions/api/chat.ts
       ├─ 请求校验与 IP 级内存限流
       ├─ 本地公开知识库：knowledge/index.json
       ├─ 证据检索与白名单过滤
       └─ DeepSeek Chat Completions API（仅服务端，环境变量密钥）
```

| 层级 | 当前方案 | 关键约束 |
| --- | --- | --- |
| 前端 | React 19 + TypeScript + Vite | 保持单页应用；页面路由由浏览器 History API 管理。 |
| 视觉与交互 | CSS、Framer Motion、Lucide、OGL | 可新增或调整页面表现，但不得借此引入新的前端运行时、SSR 或状态平台。 |
| 静态资源 | `public/` | 页面资源使用 `publicPath()` 生成路径，部署后必须可直接访问。 |
| AI 前端边界 | `src/components/AiChat.tsx` 与 `src/lib/aiClient.ts` | 浏览器只可调用 `/api/chat`，不得接触模型密钥或直接请求模型供应商。 |
| 服务端 | Cloudflare Pages Functions | `/api/chat` 为唯一公开聊天接口；函数位于 `functions/api/`。 |
| 知识库 | `knowledge/index.json` | 仅使用版本受控、公开、可核验的事实卡；每条卡必须有唯一 ID、标题、摘要、站内链接、标签与事实。 |
| 模型调用 | DeepSeek OpenAI 兼容 Chat Completions | 模型仅接收检索出的公开事实；模型输出不是可信来源。 |
| 密钥与配置 | Cloudflare Pages 环境变量 | `DEEPSEEK_API_KEY` 仅存于部署平台，禁止进入前端、仓库、日志或示例文件。 |
| 部署 | Cloudflare Pages | 构建命令为 `npm run build`，输出目录为 `dist`。 |

## 3. 不可突破的系统约束

### 3.1 AI 的证据边界

1. AI 分身是“证据型学术成果导览员”，不是通用聊天机器人，也不替本人作承诺。
2. 仅可根据检索到的 `knowledge/index.json` 公开事实生成回答；无证据时必须明确说明资料不足，不得猜测或补全。
3. 页面展示的证据链接必须由服务端根据已知事实卡生成；不得采纳模型直接生成的任意 URL 或证据 ID。
4. 模型密钥不得进入浏览器、Git 历史、公开文件、错误响应或客户端日志。
5. 不保存访问者原始提问、身份信息或长期聊天记录，除非先取得架构变更确认。

### 3.2 稳定性与安全边界

1. `/api/chat` 必须保持输入长度校验、服务端限流、错误降级和非敏感错误文案。
2. AI 服务不可用时，作品浏览、PDF 预览和联系方式必须仍可使用。
3. 外部模型调用只能从服务端函数发起；前端不得通过环境变量、代理绕过或嵌入式密钥直连。
4. 依赖升级不得在未确认的情况下改变 Node/Cloudflare 运行时兼容性或构建产物结构。

### 3.3 内容与资产边界

1. 可编辑公开资料、作品图片、PDF、证书和页面文案；更新事实卡后必须执行知识库校验。
2. 不得在未确认时删除、迁移或重命名正在被页面、知识卡或外部链接引用的公开资产。
3. 保持根路径部署（`vite.config.ts` 的 `base: "/"`），除非先完成架构变更确认。

## 4. 变更分级

### A. 可直接执行（不改变架构）

- 页面文案、色彩、排版、动画参数、组件内部交互和无障碍细节；
- 增加或替换现有 `public/` 资产，并同步更新引用；
- 在既有事实卡结构内维护公开资料；
- 修复不改变接口、数据结构、运行时和部署方式的缺陷；
- 执行现有质量检查：`npm run validate:knowledge`、`npm run build`、`npx tsc -p functions/tsconfig.json`。

### B. 必须先询问并获得明确确认

以下任一事项均属于“底层架构变更”，不得默认实施：

1. 替换或新增前端框架、路由框架、状态管理、CSS 方案、构建工具或核心运行时；
2. 将 SPA 改为 SSR/SSG、多页应用、微前端，或调整页面路由策略；
3. 更换 Cloudflare Pages、Pages Functions、部署平台、域名/重写策略或构建输出结构；
4. 新增数据库、对象存储、CMS、登录、用户账户、分析追踪、持久会话、文件上传或第三方后端；
5. 更换模型供应商、模型调用方式、提示词/检索流程的安全边界，或将关键词检索改为向量数据库/RAG 服务；
6. 修改 `/api/chat` 的公开接口、请求/响应数据结构、鉴权策略、限流策略或错误降级机制；
7. 改变知识库格式、证据白名单机制、公开资料范围或数据保留策略；
8. 引入会收集、传输或保存访客个人信息的功能；
9. 迁移、批量重命名或删除可能被知识卡、页面或外部链接使用的资源；
10. 升级或替换可能影响构建、部署或运行时兼容性的核心依赖。

### C. 确认后才可实施的必要流程

提出架构变更时，先向项目负责人说明：

1. 变更目标与原因；
2. 涉及的层级、文件和外部服务；
3. 对现有页面、链接、资料、安全、成本与运维的影响；
4. 至少一个保守替代方案；
5. 回滚方式与验证标准。

只有收到清晰的“同意实施”或等效确认后，才能修改代码、配置、依赖、部署设置或数据结构。未确认时，可提供方案、原型说明和影响评估，但不得落地变更。

## 5. 变更后的最低验收标准

在不属于架构变更的常规修改完成后，按影响范围执行：

| 变更范围 | 最低检查 |
| --- | --- |
| 页面/组件/样式 | `npm run build`，并人工检查受影响路由的桌面与窄屏表现。 |
| 知识库资料 | `npm run validate:knowledge`、`npm run build`，并确认事实卡链接可用。 |
| AI 前端或函数内部实现 | `npm run build`、`npx tsc -p functions/tsconfig.json`，并检查成功、无匹配、限流和服务故障的降级提示。 |
| 静态资产 | `npm run build`，并检查页面引用、PDF 预览与下载链接。 |

涉及 B 类事项时，验收标准应在获得确认时一并约定。

## 6. 维护约定

- 本文件是项目的架构治理入口；新增架构决定、获批例外或已完成的架构迁移，应同步更新本文件。
- 代码注释、临时需求或单次修复不得默认为对本文件的豁免。
- 当需求是否属于底层架构变更存在疑义时，按 **B 类** 处理，先询问项目负责人。

最后更新：2026-07-26
